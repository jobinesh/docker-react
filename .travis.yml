sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t jobinesh/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jobinesh/docker-react npm run test -- --coverage
